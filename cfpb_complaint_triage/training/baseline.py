"""Optional baseline trainer placeholder."""


def baseline_train(cfg):
    """Run optional baseline training (placeholder implementation)."""
    del cfg
    print(
        "Baseline training is optional and intentionally lightweight. "
        "Use the DistilBERT train command for graded flow."
    )
